export const enum InferenceOrchestratorRoute {
  INFERENCE_ORCHESTRATOR = 'inference-orchestrator',
  FILE_NAME = ':fileName',
  TRAINED_MODEL = 'trained-model',
  INFERENCE_ORCHESTRATOR_SQLITE = 'saved-inference',
}
